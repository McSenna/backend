# Dependencies
node_modules/

# Environment — secrets live here; commit only the example
.env
.env.*
!.env.example

# User uploads (ID documents, support attachments) — personal data, never commit.
# The server creates these folders itself with owner-only permissions.
storage/

# Logs
npm-debug.*
*.log

# OS
.DS_Store
